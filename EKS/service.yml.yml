apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: webapp
spec:
  selector:
    app: webapp  # ← Must match deployment labels
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  type: LoadBalancer
